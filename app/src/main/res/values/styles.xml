<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.

    -->
    <style name="AppBaseTheme" parent="android:Theme">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.

        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:fontFamily">@font/campton_book</item>
        <item name="android:buttonStyle">@style/Button</item>
        <item name="android:indeterminateTint">@color/lavender</item>
        <item name="android:dropDownListViewStyle">@style/AppTheme.DropDownList</item>
        <item name="android:textAppearancePopupMenuHeader">@style/ContextMenuHeaderAppearance</item>
        <item name="android:colorAccent">@color/lavender</item>
    </style>
    <style name="AppTheme.DropDownList" parent="Widget.AppCompat.ListView.DropDown">
        <item name="android:background">@color/bg_3</item>
    </style>
    <style name="ContextMenuHeaderAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/lavender</item>
    </style>

    <style name="StreamBaseTheme" parent="AppBaseTheme">
        <!-- Transparent streaming background to avoid extra overdraw -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Stream activity theme -->
    <style name="StreamTheme" parent="StreamBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="Button" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/bg_7</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
